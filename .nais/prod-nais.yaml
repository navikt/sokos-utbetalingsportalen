apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: sokos-okonomiportalen-frontend
  namespace: okonomi
  labels:
    team: okonomi
  annotations:
    config.linkerd.io/proxy-memory-limit: "512Mi"
    config.linkerd.io/proxy-memory-request: "128Mi"
spec:
  image: {{ image }}
  port: 7100
  liveness:
    path: /sokos-okonomiportalen-frontend/internal/isAlive
    initialDelay: 10
  readiness:
    path: /sokos-okonomiportalen-frontend/internal/isReady
    initialDelay: 10
  replicas:
    min: 2
    max: 4
  ingresses:
    - "https://okonomiportalen.intern.nav.no"
  resources:
    limits:
      cpu: "3"
      memory: 1268Mi
    requests:
      cpu: "500m"
      memory: 1024Mi
