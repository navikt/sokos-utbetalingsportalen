---
import MicrofrontendSSR from "@components/microfrontend/MicrofrontendSSR.astro";
import { getServerSideEnvironment } from "@utils/server/environment.ts";
const isLocalEnv = getServerSideEnvironment() === "local";
const appUrl = isLocalEnv ? "http://localhost:4322/" : process.env.SOKOS_UP_OPPGJORSRAPPORTER_URL;
const appAudience = isLocalEnv ? "api://dev-gcp.okonomi.sokos-up-oppgjorsrapporter/.default" : process.env.SOKOS_UP_OPPGJORSRAPPORTER_AUDIENCE;
---

<MicrofrontendSSR appTitle="Oppgjørsrapporter" appUrl={appUrl} appAudience={appAudience} />