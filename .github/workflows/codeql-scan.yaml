name: CodeQL Scan

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

env:
  TZ: Europe/Oslo

jobs:
  codeql-scan:
    permissions:
      contents: read
      security-events: write
    name: CodeQL Scan
    uses: navikt/mob-gha-workflows/.github/workflows/codeql-scan.yaml@e80242212870d8674570d82f812590617942930c # ratchet:navikt/mob-gha-workflows/.github/workflows/codeql-scan.yaml@v1.0.1
    secrets: inherit
    with:
      language: "javascript"
