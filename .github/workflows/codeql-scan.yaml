name: CodeQL Scan

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

env:
  TZ: Europe/Oslo

jobs:
  codeql-scan:
    permissions:
      contents: read
      security-events: write
    name: CodeQL Scan
    uses: navikt/mob-gha-workflows/.github/workflows/codeql-scan.yaml@ea30f1f110c126e50ad3641dedc0a530a38bf62c # ratchet:navikt/mob-gha-workflows/.github/workflows/codeql-scan.yaml@v1.0.0
    secrets: inherit
    with:
      language: "javascript"
