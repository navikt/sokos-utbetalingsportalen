name: Digestabot Update

on:
  schedule:
    - cron: "0 4 * * 1"
  workflow_dispatch:

env:
  TZ: Europe/Oslo

jobs:
  digestabot-update:
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    name: Digestabot Update
    uses: navikt/mob-gha-workflows/.github/workflows/digestabot-update.yaml@ea30f1f110c126e50ad3641dedc0a530a38bf62c # ratchet:navikt/mob-gha-workflows/.github/workflows/digestabot-update.yaml@v1.0.0
